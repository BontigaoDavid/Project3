import React from "react";

function Explanation() {
  return (
    <div className="row">
      <div className="col-12">
        <p>
          When it comes to the web, pages are all made up of elements. Elements are things like paragraphs, headings, pictures, buttons, forms, and all that good stuff. Elements are displayed as boxes, and the CSS Box Model is all about how an HTML element is displayed.
        </p>

        <p></p>
      </div>
    </div>
  );
}

export default Explanation;
